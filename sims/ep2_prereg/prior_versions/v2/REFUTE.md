# Refutation conditions v2 (pre-committed 2026-08-14, AFTER v1 REFUTED the naive
# operationalization — this is a new pre-registration, not a patch of v1)
E-P2 v2 claim: the DIFFERENTIAL signal (bistable-arm recovery time minus rigid-arm
recovery time, both under identical compression steps and independent creep) leads the
snap by >= 15% of the load range.

REFUTED if EITHER:
1. Median differential first-detection lead < 15% of the load span, >=3 of 5 seeds,
   at central timing noise (5%); OR
2. A differential-null (two rigid struts, independent creep draws, same subtraction)
   triggers detection at rate > 20%.

INCONCLUSIVE if seed-variable or non-monotone across the noise sweep.

v1 lesson recorded: absolute t-test vs own baseline cannot distinguish CSD from creep
(null fired at 96%). Differential measurement is the physical control arm made
statistical. Any change to this file after results exist marks the run EXPLORATORY.
